<div class="card border text-center">
    <h3 class="card-header">Edit Post</h3>
    <form class="card-body edit-post-form">
        <input type="hidden" name="post-id" value="{{post.id}}"/>
        <input type="hidden" name="user-id" value="{{post.user_id}}"/>

        <label for="post-title" class="form-label">Title</label>
        <input name="post-title" type="text" value="{{post.post_title}}" class="form-input" />
        <div>
            <label class="form-label" for="content">Make edits to your post body</label>
            <input style='height: 100px; width: 500px' name="post-content" type="text" value="{{post.post_content}}" class="form-input"/>
        </div>   
        <button class="btn btn-primary mt-4" type="submit" class="btn">Edit</button>
    </form>
</div>
<script src="/js/edit.js"></script>